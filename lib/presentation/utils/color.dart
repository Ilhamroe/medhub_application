import 'package:flutter/material.dart';

const Color primary = Color(0xFF0F3759);
const Color textPrimary = Color(0xFF090F47);
const Color rectangle = Color(0xFFFFA629);
const Color green = Color(0xFF00A59B);
const Color grey = Color(0xFFC4C4C4);
const Color rating = Color(0xFFFFC000);
const Color screenPage = Color(0xFFF7FBFF);
const Color dealsColor = Color(0xFFEEEEF0);
const Color saleColor = Color(0xFFFF5A5A);
const Color promoColor = Color(0xFFFFC618);
const Color carouselColor = Color(0xFFF8F8F8);
const Color ratingColor = Color.fromRGBO(0, 0, 0, 0.1);
const Color itemColor = Color.fromRGBO(0, 165, 155, 0.05);
const Color pureWhite = Color.fromARGB(255, 255, 255, 255);
const Color textColor = Color.fromRGBO(9, 15, 71, 0.45);
const Color descColor = Color.fromRGBO(9, 15, 71, 0.65);
const Color prflColor = Color.fromRGBO(9, 28, 63, 0.08);
const Color dvdColor = Color.fromRGBO(0, 0, 0, 0.05);
const Color titlePrflColor = Color.fromRGBO(9, 28, 63, 0.75);
const Color sideColor = Color.fromRGBO(224, 224, 224, 0.6);
const Color button = Color.fromRGBO(255, 255, 255, 1);
const Color border = Color.fromRGBO(65, 87, 255, 0.1);
const Color textButton = Color.fromRGBO(9, 15, 71, 0.75);
const Color eclips = Color.fromRGBO(255, 255, 255, 0.07);

const LinearGradient gradientRectangle = LinearGradient(
  colors: [
    Color.fromRGBO(27, 117, 188, 1), // Warna pertama
    Color.fromRGBO(0, 170, 245, 1), // Warna kedua
  ],
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
);

const LinearGradient categoryPink = LinearGradient(
  colors: [
    Color.fromRGBO(255, 149, 152, 1), // Warna pertama
    Color.fromRGBO(255, 112, 167, 1), // Warna kedua
  ],
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
);

const LinearGradient categoryGreen = LinearGradient(
  colors: [
    Color.fromRGBO(25, 229, 165, 1), // Warna pertama
    Color.fromRGBO(21, 189, 146, 1), // Warna kedua
  ],
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
);

const LinearGradient categoryOrange = LinearGradient(
  colors: [
    Color.fromRGBO(255, 192, 111, 1), // Warna pertama
    Color.fromRGBO(255, 121, 58, 1), // Warna kedua
  ],
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
);

const LinearGradient categoryBlue = LinearGradient(
  colors: [
    Color.fromRGBO(77, 183, 255, 1), // Warna pertama
    Color.fromRGBO(62, 125, 255, 1), // Warna kedua
  ],
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
);
const LinearGradient categoryDark = LinearGradient(
  colors: [
    Color.fromRGBO(130, 130, 130, 1), // Warna pertama
    Color.fromRGBO(9, 15, 71, 1), // Warna kedua
  ],
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
);
const LinearGradient categoryGrey = LinearGradient(
  colors: [
    Color.fromRGBO(194, 194, 194, 1), // Warna pertama
    Color.fromRGBO(87, 87, 87, 1), // Warna kedua
  ],
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
);
